# Build the Docker image for Bugzilla
docker build -t bugzilla-local .
docker build --no-cache --pull -t bugzilla-local .

# Run with no cache and recreate containers
docker-compose build --no-cache
docker-compose up -d --force-recreate